// sum of first 1-N numbers using parameterized recursion
#include<bits/stdc++.h>
using namespace std;
void sum(int n,int s)
{
    if(n <= 0){
        cout<<s;
        return ;
    }
    sum(n-1,s+n);
}
int main()
{
    int n = 10;
    int s = 0;
    sum(n,s);
}
